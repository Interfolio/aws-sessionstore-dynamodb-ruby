name: "CodeQL Advanced"

on:
  push:
    branches: [ "master" ]
    tags: ['*']
  pull_request:
    branches: [ "master" ]
  schedule:
    - cron: '46 3 * * 1'

jobs:
  code-scanning:
    uses: elsevier-research/interfolio-github-actions/.github/workflows/code-scanning.yml@main
    permissions:
      security-events: write
      packages: read
      actions: read
      contents: read
